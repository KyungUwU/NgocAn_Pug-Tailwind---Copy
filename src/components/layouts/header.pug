// src/components/layouts/header.pug

//— pull in one, single navItems declaration
include ../../../includes/variables.pug

header.sticky.top-0.bg-white.shadow.z-50
  .container.mx-auto.px-4.py-4.flex.justify-between.items-center
    a(href="/").text-2xl.font-bold.text-black VILLA

    ul.flex.space-x-6.items-center
      each navItem in navItems
        //— compute active status per iteration
        - var isActive = navItem.label === activePage
        li
          a(
            href=navItem.href,
            class= isActive
              ? 'text-orange-500 font-semibold'
              : 'hover:text-orange-500'
          ) #{navItem.label}

      //— static “Schedule a visit” button
      li
        a(
          href="#",
          class="inline-flex items-center rounded-full bg-black text-white overflow-hidden px-4"
        )
          i.fa.fa-calendar.mr-1(
            style="background-color:#F35525;color:white;width:40px;height:40px;display:flex;align-items:center;justify-content:center;border-radius:50%;"
          )
          | Schedule a visit
